# 插件市场打包说明

本文说明如何把你自己的小工具 / Python 脚本 / 依赖包，打包成「百宝箱」插件市场上可下载安装的插件。

整体流程：在本地把每个插件放进一个目录 → 用打包脚本 `scripts/package-plugins.mjs` 生成 `.zip` 归档并用**更新公钥**签名 → 脚本汇总所有插件生成 `marketplace.json` → 随发版作为 Release 资产发布。客户端（`插件市场`标签页）拉取 `marketplace.json` 后，用户点击即可下载、解压、自动落位（脚本类还会自动登记为应用）。

> 客户端托管在 Gitee 仓库 `wjqnxw/baibaoxiang-plugins`：先调 `marketplace_service::MARKETPLACE_LATEST_API`（Gitee「最新 Release」API）拿到**最新 Release 的 tag 与 `marketplace.json` 资产地址**，再把清单里各插件的下载地址自动对齐到该 tag；仅当该接口失败时才回退到固定地址 `marketplace_service::MARKETPLACE_URL`（tag `plugins`）。因此**发布时 tag 可任取**（如 `v1.0.0`），不受代码约束。App 自身更新源仍在 GitHub（`update_service::SOURCE_URL`）。

---

## 1. 三种插件与落位规则

每个插件用一个 `kind` 字段区分，决定了下载后文件放到哪、是否自动加应用：

| kind | 含义 | 解压目标目录（以安装根目录为根） | 是否自动加应用 |
|------|------|----------------------------------|----------------|
| `python_script` | Python 脚本小工具 | `<根>/pyTools/<id>/` | ✅ 是（进默认分组，解释器 `python/python.exe`，图标回退 Python 标志） |
| `program` | 程序包（如 python 运行环境的一部分） | `<根>/<id>/` | ❌ 否（仅放置文件） |
| `dependency` | 依赖包 | `<根>/python/Lib/site-packages/` | ❌ 否（仅放置文件） |

- `id` 即插件目录名，也是安装目录名与「已安装」标记的键，务必全局唯一、稳定（改名/换版本请沿用同一 `id`，仅改 `version`）。
- `python_script` 的 `entry` 指向归档内入口文件（如 `main.py`），客户端据此在 `pyTools/<id>/<entry>` 处登记应用。

---

## 2. 准备插件目录

把每个插件放进一个以 `id` 命名的子目录。脚本会自动扫描该目录下的内容打包，所以**目录里只放需要发布的文件**。

### Python 脚本示例（`pyTools/` 下，自动识别为 `python_script`）

```
pyTools/
└── file_janitor/            # id = file_janitor
    ├── main.py              # 入口（默认找 main.py，否则 <id>.py，再否则第一个 .py）
    ├── utils.py
    └── plugin.json          # 可选，覆盖名称/描述等元数据
```

### 依赖包示例（单独目录，命令行用 `--deps-dir` 指定，自动识别为 `dependency`）

```
python-deps/
└── some_pkg/                # id = some_pkg
    ├── plugin.json          # 可选
    ├── some_pkg/
    │   ├── __init__.py
    │   └── core.py
    └── README.md
```

> 依赖包建议把包目录直接放在子目录下（如上 `some_pkg/some_pkg/...`），这样解压后正好落进 `site-packages/some_pkg/`。

### 内置 Python 运行环境（`--program`，自动识别为 `program`）

仓库根的 `python/` 是整包交付的运行时，**不是**某个脚本工具，所以用 `--program` 把整个目录作为**一个**插件：

```
python/                      # id = python（目录名即 id）
├── python.exe
├── python314.dll
├── DLLs/ Lib/ Scripts/ include/
└── （此处放不了 plugin.json，见下）
```

```powershell
node scripts/package-plugins.mjs --program ../python --meta scripts/plugins-meta.json
```

生成 `python_3.14.7.zip`，客户端安装后落到 **`<安装目录>/python/`**，正是内置 Python 应在的位置；`program` 只放文件，不会自动登记为应用。

> **`python/` 里没法放 `plugin.json`**：它是联接到 `C:\Program Files\Python\Python314` 的**目录联接（只读）**，写入会被拒绝。元数据因此放在外部文件 `toolbox/scripts/plugins-meta.json`：
>
> ```json
> {
>   "python": {
>     "name": "内置 Python 运行环境",
>     "description": "Python 3.14.7 运行时（含 pip）…",
>     "version": "3.14.7"
>   }
> }
> ```
>
> `--meta` 的优先级高于包目录内的 `plugin.json`，key 用插件 id。

> **体积提醒**：`python/` 约 393 MB（指向系统安装的 Python 3.14），打包后的 zip 仍很大，注意 Gitee Release 的**单文件大小限制**。只发布 `pyTools` 脚本更新时可以不带 `--program`。

---

## 3. `plugin.json` 元数据（可选）

每个插件子目录里放一个 `plugin.json` 可覆盖默认元数据。所有字段均可选，缺省时脚本按目录名/类型推断。

```json
{
  "name": "文件清理助手",          // 展示名，缺省用目录名
  "description": "清理临时文件与回收站",  // 展示描述，缺省空
  "author": "weeds",             // 作者，缺省空
  "version": "1.0.0",            // 版本号，缺省取命令行 --tag 去掉前缀 v
  "kind": "python_script",       // python_script / program / dependency，缺省按目录归属
  "entry": "main.py",            // 入口文件（python_script/program 需要），缺省自动探测
  "launch_kind": 1,              // 0 程序 / 1 Python / 2 命令 / 3 网页，缺省 python_script=1 其它=0
  "interpreter": "python/python.exe",  // Python 类解释器相对路径，缺省 python_script=python/python.exe
  "icon_url": "https://.../icon.png",   // 可选图标（PNG），缺省 python_script 客户端显示 Python 标志
  "python_requirement": ">=3.9"  // 可选版本要求，仅展示
}
```

> 打包脚本**不生成** `auto_add` 字段；客户端默认仅 `python_script` 自动加应用。`kind=program/dependency` 如需强制自动加应用，可在生成的 `marketplace.json` 里手动给该插件加 `"auto_add": true`（后端支持此覆盖字段）。

---

## 4. 一键打包

脚本：`scripts/package-plugins.mjs`（Node ESM，无需额外依赖，打包用 Windows 自带 `tar.exe`）。

```powershell
cd toolbox

node scripts/package-plugins.mjs `
  --repo wjqnxw/baibaoxiang-plugins `
  --tag plugins `
  --plugins-dir ../pyTools `
  --deps-dir ../python-deps `
  --out dist/plugins
```

参数说明：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--repo` | `wjqnxw/baibaoxiang-plugins` | `owner/name`，用于拼下载地址（Gitee 仓库） |
| `--tag` | `plugins` | Release 标签，仅用于拼 `download_url` 路径（客户端会按最新 Release 的 tag 自动纠正，故与 Gitee 上的实际 tag 不一致也能用）；版本号默认取 `plugin.json` 的 `version`，无则回退 `1.0.0` |
| `--plugins-dir` | `pyTools` | 扫描其中的子目录作为 `python_script` 插件 |
| `--deps-dir` | 无 | 可选，扫描其中的子目录作为 `dependency` 插件 |
| `--program` | 无 | 可选、**可重复**；目录【本身】为一个 `program` 插件（dirname 即 id）。用于整包交付的运行时，如 `--program ../python` |
| `--meta` | 无 | 可选，外部元数据文件（JSON：`{ "<id>": { name, description, version, ... } }`），优先级高于包内 `plugin.json` |
| `--out` | `dist/plugins` | 输出 `.zip` 归档与 `marketplace.json` 的目录 |
| `--no-sign` | 关闭 | 跳过签名（仅本地预览用，见下） |

脚本会：
1. 对每个子目录用 `tar.exe -a -cf <out>/<id>_<version>.zip -C <dir> .` 打包；
2. 用 `npx tauri signer sign` 对 zip 签名，得到 `<zip>.sig`，读取其内容为 `signature`；
3. 汇总生成 `marketplace.json`（含每个插件的 `download_url`、签名、元数据）到 `--out`。

---

## 5. 签名（发布必须）

客户端在配置了更新公钥（`update_service::PUBLIC_KEY`）时，**要求插件必须带签名**，否则拒绝安装。签名与现有版本更新共用同一对密钥：

```powershell
# 首次生成密钥对（已生成则跳过），会输出 key.pub 与私钥内容
npx tauri signer generate
```

打包前设置私钥环境变量（务必与生成时一致），脚本会自动读取：

```powershell
$env:TAURI_SIGNING_PRIVATE_KEY = "你的私钥内容"
$env:TAURI_SIGNING_PRIVATE_KEY_PASSWORD = "私钥密码（未设可省略）"
node scripts/package-plugins.mjs --repo ... --tag ...
```

- `marketplace.json` 里的 `signature` 即 `.sig` 文件内容（minisign 文本格式），客户端用 `key.pub` 验签。
- 若未设私钥且未加 `--no-sign`，脚本会报错退出。

---

## 6. 发布

### 方式 A：本地预览（不签名）
加上 `--no-sign`，生成的 `marketplace.json` 中 `signature` 为空，仅用于本地核对字段/目录结构。

### 方式 B：自动打包 + 手动发布到 Gitee（当前方案）
`.github/workflows/release.yml` 在推 `v*` tag 后自动构建 App，并跑插件打包脚本（读取 `TAURI_SIGNING_PRIVATE_KEY[_PASSWORD]` secret，默认扫描 `../pyTools` 与 `../python-deps`），把插件归档与 `marketplace.json` 生成到 `dist/plugins/`，**下载地址已指向 Gitee**。但 **App 更新包仍随发版自动发布到 GitHub Release**（`latest.json` 等），插件市场与 App 更新源分属两个平台。

插件市场的 `dist/plugins/*` **不会**自动上传到 GitHub；请你在 Gitee 仓库 `wjqnxw/baibaoxiang-plugins` 手动建一个 Release（tag 可任取，例如 `v1.0.0`），把 `dist/plugins/` 下的**全部**内容作为资产上传：`marketplace.json` + 每个 `.zip` **及其同名 `.sig`**。

> **必须传全**：客户端按清单逐条下载，清单里有而 Release 里没有的条目会 404（只传一部分就会出现「有的插件能装、有的报 404」）。上传完可用这个办法自查：把 Release 资产名与 `marketplace.json` 里的 `download_url` 文件名逐一比对，数量应一致。
>
> **tag 不必叫 `plugins`**：客户端会用「最新 Release」API 自动定位并用其 tag 纠正清单中的下载地址；但请保证该 Release 是**最新**的一个（Gitee 按创建时间取 latest）。

> 注意：`release.yml` 里 `--plugins-dir ../pyTools` 与 `--deps-dir ../python-deps` 是相对 `toolbox/` 的路径（仓库根下）。请确认这两个目录存在且内容就绪，再打 tag。Gitee Release 资产需含同名 `.sig` 签名文件，否则客户端在配置了公钥时会拒绝安装。

---

## 7. `marketplace.json` 结构（自动生成，供参考）

```json
{
  "schema": 1,
  "updated_at": "2026-09-20T10:00:00.000Z",
  "plugins": [
    {
      "id": "file_janitor",
      "name": "文件清理助手",
      "description": "清理临时文件与回收站",
      "version": "1.0.0",
      "author": "weeds",
      "kind": "python_script",
      "download_url": "https://gitee.com/wjqnxw/baibaoxiang-plugins/releases/download/plugins/file_janitor_1.0.0.zip",
      "signature": "untrusted comment: signed by ...\n<base64 签名>",
      "icon_url": null,
      "entry": "main.py",
      "launch_kind": 1,
      "interpreter": "python/python.exe",
      "python_requirement": ">=3.9"
    }
  ]
}
```

---

## 8. 注意事项

- **归档单层目录会被剥离**：若 zip 内只有一个顶层目录（如 `file_janitor/`），解压时会把其下内容直接提到目标目录，避免 `pyTools/<id>/<id>/...` 双重嵌套。多个顶层文件则原样保留。
- **依赖包不要套多余层**：让它解压后正好是 `site-packages/<pkg>/...`。
- **改版不改 id**：升级只改 `version` 与 `download_url` 指向的新 zip，保持 `id` 不变，客户端才能正确显示「已安装（v 旧版）」并提示更新。
- **图标**：`icon_url` 必须是 PNG；不填时 `python_script` 应用在前端显示 Python 官方标志（规避解释器图标问题）。
- **校验**：客户端读取「已安装」状态靠写入 `<根>/.marketplace/<id>.json` 标记，无需联网即可标识；列表来自远程 `marketplace.json`。
