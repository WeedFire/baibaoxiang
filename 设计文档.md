# 百宝箱（Windows 应用快速启动与管理工具）详细开发设计文档 V2.0

**版本**：V2.0  
**日期**：2026-06-24  
**目标平台**：Windows 10 1809+ / Windows 11 (x64)  
**技术栈**：Tauri 2.0 (Rust 后端) + React + TypeScript + Vite  
**部署方式**：便携版 (.exe 单文件) / MSI 安装包  

---

## 1. 项目概述

### 1.1 背景与目标
在日常工作中，用户需要在多个应用程序、脚本、系统工具间频繁切换。传统开始菜单或桌面快捷方式缺乏灵活的分组、自定义布局和便捷管理能力。针对开发者和高级用户，他们需要将命令行工具、Python 脚本、系统设置等统一收纳到一个界面，并能自由组织、快速启动。

“百宝箱” 是一款基于 Rust 与 Web 前端技术构建的 **Windows 应用快速启动与管理面板**。它充分利用 Tauri 的轻量、安全特性，结合 React 的灵活 UI，为用户提供高性能、低资源占用的原生桌面体验。

### 1.2 核心价值
- **统一入口**：集中管理散落各处的应用、脚本、工具，一键启动。
- **灵活分组**：自定义分组，按项目、功能组织应用。
- **可视化布局**：自动网格排列与手动像素级自由拖拽布局，支持锁定、重置。
- **悬浮操作**：鼠标悬停图标时显示快捷操作按钮（编辑、删除、管理员运行等）。
- **Python 深度集成**：为 `.py` 应用绑定专属 Python 解释器，启动时自动调用。
- **高性能低占用**：得益于 Rust 后端，启动速度 < 1 秒，内存占用 < 50MB。

---

## 2. 技术架构

### 2.1 整体架构分层

```
┌─────────────────────────────────────────────────────────┐
│                  前端表现层 (React + TS)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐ │
│  │ MainPage │ │ AddDialog│ │ Settings │ │ TrayManager│ │
│  │ (主界面)  │ │ (编辑)   │ │ (设置页) │ │ (托盘控制) │ │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └─────┬──────┘ │
│       └─────────────┼────────────┼─────────────┘         │
│                     │ Tauri IPC (invoke)                 │
├─────────────────────▼───────────────────────────────────┤
│                     Tauri Rust 后端                       │
│  ┌──────────────────────────────────────────────────┐   │
│  │  命令处理层 (Commands)                            │   │
│  │  - app_management (增删改查)                     │   │
│  │  - group_management                              │   │
│  │  - layout_management                             │   │
│  │  - python_detection                              │   │
│  │  - process_launcher                              │   │
│  │  - icon_extractor                                │   │
│  └──────┬───────────────────────────────────────────┘   │
│         │                                                │
│  ┌──────▼───────────────────────────────────────────┐   │
│  │  核心服务层 (Services)                            │   │
│  │  - DataService (SQLite CRUD)                    │   │
│  │  - PythonEnvDetector                              │   │
│  │  - ProcessLauncher                                │   │
│  │  - IconService                                    │   │
│  │  - LayoutEngine                                   │   │
│  └──────┬───────────────────────────────────────────┘   │
│         │                                                │
│  ┌──────▼───────────────────────────────────────────┐   │
│  │  数据访问层 (DAL)                                 │   │
│  │  - SQLite (rusqlite) + 迁移管理                  │   │
│  │  - 配置文件 (serde + JSON)                       │   │
│  └──────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│              平台抽象与基础设施                           │
│  - windows-rs (API 调用)  - global_hotkey (全局热键)    │
│  - tray-icon (托盘)       - auto_launch (开机自启)      │
└─────────────────────────────────────────────────────────┘
```

**关键选型说明**：  
- **Tauri 2.0**：提供 Rust 至前端的安全 IPC，极小的包体积（前端资源 + Rust 二进制 < 10MB）。  
- **React + TypeScript**：构建复杂交互的应用程序界面，生态丰富。  
- **SQLite (rusqlite)**：轻量、无服务器，适合本地数据持久化。  
- **CSS Modules / Tailwind CSS**：快速实现主题化、响应式布局。

### 2.2 项目目录结构
```
toolbox/
├── src-tauri/               # Rust 后端
│   ├── src/
│   │   ├── main.rs          # 入口，构建 Tauri 应用
│   │   ├── commands/        # Tauri 命令模块
│   │   │   ├── mod.rs
│   │   │   ├── app_cmd.rs
│   │   │   ├── group_cmd.rs
│   │   │   ├── layout_cmd.rs
│   │   │   └── system_cmd.rs
│   │   ├── services/        # 核心业务服务
│   │   │   ├── mod.rs
│   │   │   ├── data_service.rs
│   │   │   ├── icon_service.rs
│   │   │   ├── python_detector.rs
│   │   │   ├── process_launcher.rs
│   │   │   └── layout_engine.rs
│   │   ├── models/          # 数据模型
│   │   ├── db/              # 数据库迁移与连接
│   │   └── utils/           # 工具函数
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                     # React 前端
│   ├── components/          # 可复用组件
│   │   ├── AppIcon.tsx
│   │   ├── LayoutCanvas.tsx
│   │   ├── GroupTabs.tsx
│   │   └── ...
│   ├── pages/               # 页面组件
│   ├── hooks/               # 自定义 hooks
│   ├── store/               # 状态管理 (zustand)
│   ├── styles/              # 样式
│   └── App.tsx
├── package.json
└── vite.config.ts
```

---

## 3. 核心模块详细设计

### 3.1 数据模型与存储（Rust 端）

#### 数据库表结构
```sql
CREATE TABLE app_groups (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    sort_order INTEGER NOT NULL DEFAULT 0,
    is_default INTEGER NOT NULL DEFAULT 0  -- 0/1 bool
);

CREATE TABLE app_items (
    id TEXT PRIMARY KEY,
    group_id TEXT NOT NULL,
    name TEXT NOT NULL,
    executable_path TEXT NOT NULL,
    arguments TEXT DEFAULT '',
    working_directory TEXT DEFAULT '',
    startup_window_style INTEGER NOT NULL DEFAULT 0,  -- 0=Normal,1=Max,2=Min
    is_python_script INTEGER NOT NULL DEFAULT 0,
    python_interpreter_path TEXT DEFAULT '',
    show_console INTEGER NOT NULL DEFAULT 0,
    run_as_admin INTEGER NOT NULL DEFAULT 0,
    allow_multiple_instances INTEGER NOT NULL DEFAULT 1,
    icon_path TEXT DEFAULT '',
    sort_order INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY(group_id) REFERENCES app_groups(id) ON DELETE CASCADE
);

CREATE TABLE layout_info (
    app_id TEXT PRIMARY KEY,
    pos_x REAL NOT NULL DEFAULT 0,
    pos_y REAL NOT NULL DEFAULT 0,
    FOREIGN KEY(app_id) REFERENCES app_items(id) ON DELETE CASCADE
);

CREATE TABLE user_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

#### Rust 数据模型
```rust
// models/app.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct AppItem {
    pub id: String,
    pub group_id: String,
    pub name: String,
    pub executable_path: String,
    pub arguments: Option<String>,
    pub working_directory: Option<String>,
    pub startup_window_style: WindowStyle,
    pub is_python_script: bool,
    pub python_interpreter_path: Option<String>,
    pub show_console: bool,
    pub run_as_admin: bool,
    pub allow_multiple_instances: bool,
    pub icon_path: Option<String>,
    pub sort_order: i32,
    pub created_at: String,
    pub updated_at: String,
}

#[derive(Debug, Serialize, Deserialize, Clone)]
pub enum WindowStyle {
    Normal = 0,
    Maximized = 1,
    Minimized = 2,
}
```

### 3.2 命令接口设计（Tauri IPC）

所有前端操作通过 `invoke` 调用 Rust 命令。示例命令：

| 命令 | 描述 |
|------|------|
| `get_apps_by_group(group_id)` | 获取分组下的应用列表 |
| `add_app(app_data)` | 添加新应用 |
| `update_app(app_data)` | 更新应用信息 |
| `delete_app(app_id)` | 删除应用 |
| `get_groups()` | 获取所有分组 |
| `create_group(name)` | 创建分组 |
| `launch_app(app_id)` | 启动应用（内部处理 Python 等） |
| `detect_python()` | 检测系统 Python 路径 |
| `extract_icon(file_path)` | 提取 exe 图标，返回 base64 或文件路径 |
| `save_layout(app_id, x, y)` | 保存手动布局位置 |
| `export_config()` | 导出配置为 JSON 文件 |
| `import_config(path)` | 从 JSON 文件导入配置 |

前端调用示例：
```ts
import { invoke } from '@tauri-apps/api/core';

const apps = await invoke<AppItem[]>('get_apps_by_group', { groupId: 'default' });
```

### 3.3 布局引擎（Rust + 前端协同）

**自动网格模式**：由前端计算位置，无需存储坐标。前端根据 `sort_order` 排序，使用 CSS Grid 或 flexbox 自动排列。  
**手动自由模式**：每个图标的像素坐标存储在 `layout_info` 表。前端使用 `react-dnd` 或自定义拖拽，移动结束时调用 `save_layout` 保存坐标。Rust 端负责数据持久化，前端负责渲染。

**拖拽实现要点**：  
- 手动模式下，外层容器为 `position: relative`，每个图标为 `position: absolute`，其 `left`、`top` 来自后端数据。  
- 使用 `pointer` 事件实现拖拽，避免与操作系统手势冲突。  
- 拖拽过程中，图标跟随鼠标移动，松手时更新状态并调用 Rust 命令保存。

**布局锁定与重置**：  
- 锁定状态存储于前端内存或用户设置中，锁定时忽略拖拽事件。  
- 重置时，前端删除该分组下所有 `layout_info` 记录，图标回归自动网格排列。

### 3.4 Python 集成与进程启动（Rust 后端）

**检测系统 Python**：
```rust
// services/python_detector.rs
use std::process::Command;

pub fn detect_system_python() -> Option<String> {
    let candidates = ["python", "python3", "py"];
    for cmd in &candidates {
        if let Ok(output) = Command::new(cmd).arg("--version").output() {
            if output.status.success() {
                return Some(cmd.to_string());
            }
        }
    }
    None
}
```

**启动应用（处理 Python 脚本）**：
```rust
// services/process_launcher.rs
pub fn launch_app(app: &AppItem) -> Result<(), String> {
    let (program, args) = if app.is_python_script {
        let interpreter = app.python_interpreter_path.as_deref().unwrap_or("python");
        let mut args = vec![app.executable_path.as_str()];
        if let Some(ref extra) = app.arguments {
            args.extend(extra.split_whitespace());
        }
        (interpreter.to_owned(), args)
    } else {
        let args = app.arguments.as_deref()
            .map(|a| a.split_whitespace().collect())
            .unwrap_or_default();
        (app.executable_path.clone(), args)
    };

    let mut cmd = Command::new(&program);
    cmd.args(&args);
    if let Some(ref wd) = app.working_directory {
        cmd.current_dir(wd);
    }
    if app.run_as_admin {
        // Windows 需要 shell 执行并添加 runas
        // 简化：通过 ShellExecuteW 或 使用 runas 动词
        // 示例略，实际需调用 windows-rs 的 ShellExecuteW
    }
    // 窗口样式通过 CreateProcess 标志设置，此处简化
    cmd.spawn().map_err(|e| e.to_string())?;
    Ok(())
}
```

**提取 EXE 图标**：  
利用 `windows-rs` 调用 `ExtractAssociatedIconW`，将图标转换为 PNG 字节或保存到临时目录，返回可访问的 URL 供前端使用。

### 3.5 前端核心组件设计

**主页面 (MainPage)**  
- 顶部工具栏：搜索框、分组标签（可拖拽排序分组）、布局模式切换、锁定按钮、设置入口。
- 中心图标区：  
  - 自动模式：`div` 使用 CSS Grid，通过 `grid-template-columns: repeat(auto-fill, minmax(100px, 1fr))` 实现自适应网格。
  - 手动模式：切换为 `div` (相对定位)，每个 `AppIcon` 为绝对定位，坐标由 Rust 数据驱动。
- 悬浮菜单：每个 `AppIcon` 内包含一个 `HoverCard` 组件（或纯 CSS `:hover` 弹出层），显示编辑、删除、管理员运行、打开文件位置等按钮。
- 右键菜单：通过 `onContextMenu` 事件展示自定义菜单。

**应用图标组件 (AppIcon)**  
- 显示图标图片（来自 `icon_path` 或默认图标）、名称。  
- 支持拖拽：手动模式下为 `draggable`，通过 `react-dnd` 或原生事件处理。
- 单击启动：调用 `launch_app` 命令。
- 双击编辑：弹出编辑对话框。

**添加/编辑对话框**  
- 表单字段：名称、路径（带文件选择对话框）、参数、工作目录、启动窗口样式。
- Python 脚本勾选框：勾选后显示解释器路径输入框及“检测”按钮。
- 分组选择下拉框、图标预览及自定义上传。

**设置页面**  
- 通用：开机自启（通过 Tauri 的 `auto_launch` 插件）、全局热键（调用 Rust 注册）、最小化到托盘。
- 外观：主题（浅/深/跟随系统）、图标大小、网格间距。
- 数据：导出/导入配置（调用 Rust 命令生成/读取 JSON 文件）。

### 3.6 托盘与全局热键
- **系统托盘**：使用 Tauri 的 `tray-icon` 插件（Rust 端配置），右键菜单包含“显示主窗口”和“退出”。
- **全局热键**：Rust 端使用 `global_hotkey` crate 注册热键，监听事件发送到前端（通过 Tauri 事件），前端切换窗口可见性。

---

## 4. 非功能性需求实现

### 4.1 性能
- **启动速度**：Tauri 应用冷启动通常在 500ms 内；首次加载前端资源后缓存。
- **UI 流畅**：拖拽时使用 `requestAnimationFrame` 更新位置，避免卡顿；图标列表虚拟化（如仅在视口内渲染）。
- **内存占用**：Rust 管理数据，React 仅渲染界面，空闲内存 < 50MB。

### 4.2 兼容性与部署
- 使用 Tauri 2.0 打包为 `.msi` 或 `.exe` 安装程序（通过 `tauri-bundler`）。便携版直接提供解压即用的文件夹。
- 依赖 WebView2（Windows 10 1809+ 已内置），无需额外安装运行库。
- 最终安装包体积 < 15MB。

### 4.3 安全
- 应用的路径信息仅保存在用户目录 `%APPDATA%\toolbox` 下，权限隔离。
- 启动应用通过 Rust 的 `Command`，不暴露给前端直接操作系统命令，防止注入。
- 管理员权限启动时，通过 Windows API 的 `ShellExecuteW` 并指定 `runas` 动词，由 UAC 控制。

### 4.4 可维护性
- 前端采用组件化，状态管理集中（zustand），与后端通过类型化的 Tauri 命令交互。
- Rust 后端分层清晰（Commands → Services → DAL），便于测试与扩展。
- 数据库迁移使用内嵌 SQL 脚本或 `refinery` 等迁移工具。

---

## 5. 开发路线图

| 阶段 | 时间 | 核心任务 | 里程碑 |
|------|------|----------|--------|
| MVP | 3周 | Tauri 项目搭建，SQLite 集成，CRUD 命令实现，前端自动网格布局，单击启动，基本分组 | 可运行的基础版 |
| 增强 | 2周 | 手动拖拽布局、悬浮/右键菜单、Python 检测与启动、图标提取、导入导出 | 功能完整的内测版 |
| 打磨 | 2周 | 主题切换、性能优化、全局热键、系统托盘、安装包制作、错误处理、用户文档 | 可发布的 1.0 版 |

---

## 6. 总结

“百宝箱” V2.0 采用 Rust + Tauri + React 技术栈，在保留原有全部功能的基础上，利用 Rust 的内存安全与高性能，以及 Web 前端的丰富交互能力，打造了一款轻量、快速、安全的 Windows 应用启动管理工具。模块化的架构设计保证了后续功能扩展的灵活性，也符合现代桌面应用开发的最佳实践。

---

**文档版本**：2.0  
**最后更新**：2026-06-24  
**维护者**：百宝箱开发团队